export interface LocationOBJ {
    id: number;
    name: string;
    latitude: string;    // You could make this number if your backend sends it that way later
    longitude: string;
    description: string;
  }